# Project Configuration for Bursting Bubble Simulation
# This file is auto-generated by reset_install_requirements.sh
# Run that script first to set up BASILISK paths

# If you have a local basilisk installation, uncomment and edit:
# export BASILISK=/path/to/your/basilisk/src
# export PATH=$PATH:$BASILISK

# For local project basilisk (created by reset_install_requirements.sh):
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [ -d "$SCRIPT_DIR/basilisk/src" ]; then
    export BASILISK="$SCRIPT_DIR/basilisk/src"
    export PATH=$PATH:$BASILISK
else
    echo "WARNING: basilisk/src not found. Run reset_install_requirements.sh first." >&2
fi
